import React from 'react';

const style = {
    wrapper: `flex justify-center`
}

const SignIn = () => {
    return (
        <div className={style.wrapper}>

        </div>
    );
};

export default SignIn;